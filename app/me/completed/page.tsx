import { PersonalCollection } from "@/components/personal-collection";
export default function CompletedPage() { return <PersonalCollection type="completed" title="我的完成" description="同一幅拼图只记录一次" empty="还没有完成拼图" />; }
