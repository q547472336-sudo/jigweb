import { fail, jsonBody, ok } from "@/lib/domain/api-contract";
import { getUser, store } from "@/lib/server/local-store";
export async function POST(request: Request) { const userId = getUser(request); if (!userId) return fail("AUTH_REQUIRED", "请先登录", 401); const body = await jsonBody(request); const description = typeof body?.description === "string" ? body.description.trim() : ""; if (!description || description.length > 1000) return fail("VALIDATION_ERROR", "反馈内容需为 1～1000 个字符", 422, false, "description"); const ticketNo = `FB-${Date.now().toString(36).toUpperCase()}`; const item = { id: crypto.randomUUID(), userId, type: typeof body?.type === "string" ? body.type : "other", description, ticketNo, createdAt: new Date().toISOString() }; store.feedback.push(item); return Response.json(ok({ ticketNo }), { status: 201 }); }
